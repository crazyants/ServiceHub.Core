@model LoggedOutViewModel

@{
    // set this so the layout rendering sees an anonymous user
    ViewData["signed-out"] = true;
}

<link rel="import" href="~/elements/page-logout/page-logout.html" />

<page-logout redirect-uri="@Model.PostLogoutRedirectUri"
             client-id="@Model.ClientName"></page-logout>